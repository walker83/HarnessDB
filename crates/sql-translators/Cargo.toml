[package]
name = "sql-translators"
version.workspace = true
edition.workspace = true
description = "SQL dialect translators for HarnessDB database chameleon (MaxCompute, Hologres, etc.)"

[dependencies]
tracing = { workspace = true }
thiserror = { workspace = true }
regex = "1"

[dev-dependencies]
