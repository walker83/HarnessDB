# 2026-05-10 RorisDB Bug排查

## 任务
用户要求继续找bug（至少10个），修复编译，并测试数据是否真实存储了。

## 找到的Bug（15个）

| # | 位置 | Bug描述 | 状态 |
|---|---|---|
| 1 | `catalog.rs` | `TableProvider` import缺失 | ✅ 已修复 |
| 2 | `writer.rs:11` | MAGIC常量拼写错误+字节数错误 | ✅ 已修复 |
| 3 | `reader.rs:436` | null值处理错误 | ✅ 已修复 |
| 4 | `engine.rs:331` | 默认路径拼写错误 | ✅ 已修复 |
| 5 | `common.rs:22` | `Table`初始化缺少`name:`字段 | ✅ 已修复 |
| 6 | `common.rs:74` | `Table`初始化`tablet_id`重复 | ✅ 已修复 |
| 7 | `catalog.rs:272` | `Table`初始化缺少`tablet_id:` | ✅ 已修复 |
| 8 | `schema_provider.rs:201` | `Table`初始化缺少`tablet_id:` | ✅ 已修复 |
| 9 | `information_schema.rs:405` | `Table`初始化缺少`tablet_id:` | ✅ 已修复 |
| 10 | `lib.rs:309` | `Table`初始化缺少`tablet_id:`(字段速记语法) | ❌ 待修复 |
| 11 | `fe_main.rs:462` | `Table`初始化缺少`tablet_id:` | ❌ 待修复 |
| 12 | `fe_main.rs` | `create_table()`调用错误 | ❌ 待修复 |
| 13 | `fe_main.rs` | `get_mem_table()`调用错误 | ❌ 待修复 |
| 14 | `table_provider.rs` | `scan()`返回`EmptyExec` | ❌ 待修复 |
| 15 | `block_convert.rs` | `record_batch_to_block()`和`array_to_vector()`是stub | ❌ 待修复 |

## 剩余编译错误

1. `benches/tpch/src/lib.rs:309` - `Table {` 初始化缺少 `tablet_id:` 字段（Python脚本处理字段速记语法`id,`失败）
2. `roris-server/src/fe_main.rs:462` - `Table {` 初始化缺少 `tablet_id:` 字段（`Edit`工具一直失败）
3. `roris-server/src/fe_main.rs` - `create_table()` 和 `get_mem_table()` 方法调用错误（`RorisCatalogProvider` 没有这些方法）
4. `tpch-bench` - `tokio` crate未链接，函数未找到

## 核心问题

**数据是否真实存储了？**
- 目前`scan()`返回`EmptyExec`，所以SELECT查询不到任何数据。
- 存储引擎的写入/读取代码已实现，但因为编译不通过，无法测试验证。
- 需要：①修复编译错误 ②让`scan()`返回真实数据（`MemoryExec`或直接返回`RecordBatch`）

## 下一步

1. 修复`Table`初始化问题——给`Table`实现`Default` trait，然后在初始化用`..Default::default()`
2. 修复`RorisCatalogProvider`的方法调用错误——实现`create_table()`和`get_mem_table()`方法，或修改调用代码使用`CatalogManager`
3. 修复`table_provider.rs::scan()`让它返回真实数据
4. 编译通过后，写集成测试验证数据是否真实存储和查询

## 备注

- 一直在手动修复`Table`初始化的编译错误，但Python脚本处理字段速记语法（`id,`）一直失败
- `Edit`工具多次说"string to replace not found"，需要换用`Bash`+`sed`或`Write`工具
- 已经花费大量时间（3+小时）在修复编译错误上
