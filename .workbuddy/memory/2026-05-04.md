# 2026-05-04

## 项目评估
- 对 HarnessDB（Rust OLAP 数据库，灵感来自 Apache Doris）进行了全面代码审查
- 项目地址：/Users/walker/code/HarnessDB
- 架构：FE（前端）+ BE（后端）MPP 架构，20K+ 行零 unsafe Rust 代码
- 支持 SQL 解析、查询规划、优化器（规则-based + CBO）、向量化表达式引擎、
  列式存储、MySQL 协议、分布式查询调度、gRPC FE-BE 通信

## 第1轮评价（16:13）
- 结构：清洁的 crate 划分（fe-*, be-*, types, common, proto, rpc）
- 代码质量：零 unsafe、宏模式设计良好、Bitmap 使用 trailing_zeros 迭代性能好
- 短板：存储引擎读路径基本为空（SegmentReader::read_page 返回空 vec），
  执行算子过于简化（Filter 是 mock），NULL 语义不正确（默认值替代传播）

## 第2轮评价（22:43）- 用户提交了大量新代码
- 20 个新 commit，4983 行新增，46 个文件改动
- 新功能包括：
  1. Runtime Filter（Bloom/MinMax/In 三种类型）
  2. Materialized View DDL + 查询改写
  3. UPDATE/DELETE BE 执行层
  4. P0 分区支持（Range/List/Hash）
  5. CBO 成本模型 + 统计信息采集 + ANALYZE TABLE
  6. Parquet 文件格式支持
  7. ANN Index（向量相似度搜索）
  8. Inverted Index（全文搜索）
  9. Bitmap Index（RoaringBitmap）
  10. Float32Array 向量数据类型
  11. TypedVector trait + 批处理统计函数
  12. Predicate Parser（587行，谓词解析→存储级谓词）
  13. ExecutionPlan 枚举（13 种节点类型）
  14. async_trait 异步执行节点
  15. Runtime Filter 集成到 ScanExecNode
  16. Clippy warnings 从 189 降至 33
  17. 中英文文档 50 篇 + 详细路线图
